name: hello
on: [ workflow_dispatch ]
jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Hello
        run: echo "Hello"

      - name: Deploy
        if: ${{ github.ref == 'refs/heads/main' }}
        run: echo "Deploying..."